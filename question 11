#include <stdio.h>
#include<math.h>
int main()
{
    int a,b,c,s;
    scanf("%d%d%d",&a,&b,&c);
    s=(a+b+c)/2;
    int z= sqrt(s*(s-a)*(s-b)*(s-c));
    printf("area of square is %d",z);

    return 0;
}
